name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: globalcenterofexcellence.com
          username: gitmain@quazi.global
          password: Tanvir@@114
          port: 22
          protocol: sftp
          security: loose
          timeout: 600000

