name: 🚀 Deploy website on push

on:
  push:
    branches:
      - main   # অথবা master

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ftp.globalcenterofexcellence.com
          username: gitmain@quazi.global
          password: Tanvir@@114
          port: 21
          protocol: ftp

